<form method="post" action="{{ path('app_sinstre_delete', {'id': sinstre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sinstre.id) }}">
    <button class="btn"style="color: blue">Delete</button>
</form>
